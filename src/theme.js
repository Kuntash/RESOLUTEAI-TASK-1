import { createTheme } from "@mui/system";

const theme = createTheme({
    palette: {
        primary: {
            main: "#fa2611"
        },
        secondary: {
            main: "rgba(0,0,0,0.16)"
        }
    }
});

export default theme;